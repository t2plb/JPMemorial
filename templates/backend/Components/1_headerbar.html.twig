<nav class="navbar navbar-expand-lg fixed-top navbar-{{ theme|raw }}" {{ stimulus_target('darkbutton', 'navbar') }}>
        <!-- Left navbar links -->
        <ul class="navbar-nav">
            <li class="nav-item d-none d-sm-inline-block">
                <a href="{{ path('app_main') }}" class="nav-link">JPM Back Office</a>
            </li>
        </ul>


        <!-- Right navbar links -->
        <ul class="navbar-nav ml-auto">
            <li class="nav-item d-none d-xl-block">
                <a class="nav-link" href="{{ path('app_main') }}" role="button">
                    <i class="fas fa-home"></i>
                </a>
            </li>
            <li class="nav-item d-none d-xl-block mr-3">
                <a class="nav-link" href="{{ path('app_logout') }}" role="button">
                    <i class="fas fa-sign-out-alt"></i>
                </a>
            </li>
            <li class="nav-item d-sm-block darkbutton mr-3">
                <input id="toggle" class="toggle darkbutton" type="checkbox" {{ stimulus_target('darkbutton', 'checkbox') }} {{ stimulus_action('darkbutton', 'toggle') }}
                        {% if theme == 'light' %}
                            checked
                        {% endif %}
                >
            </li>
        </ul>
    </nav>